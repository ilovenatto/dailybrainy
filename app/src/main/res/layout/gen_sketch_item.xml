<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="sketch"
            type="org.chenhome.dailybrainy.repo.game.Sketch" />

        <variable
            name="listener"
            type="org.chenhome.dailybrainy.ui.sketch.VoteSketchAdapter.Listener" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="@{()->listener.onClick(sketch)}"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageSketch"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_weight="1"
            tools:srcCompat="@tools:sample/backgrounds/scenic" />

        <TextView
            android:id="@+id/textVotes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text='@{sketch.idea.votes + " vote(s)"}'
            tools:text="3 votes" />
    </LinearLayout>
</layout>
